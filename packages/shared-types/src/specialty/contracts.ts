export type Specialty = {
  id: string;
  name: string;
  description?: string;
  isActive: boolean;
  createdAt: string;
  updatedAt: string;
};

export type SpecialtySummary = {
  id: string;
  name: string;
};
