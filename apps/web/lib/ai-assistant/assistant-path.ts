/**
 * The assistant screen. Shared by the launcher, the top-bar link, the sidebar
 * entry, and the unread bookkeeping — all four have to agree on "is the user
 * looking at the assistant right now", and a second copy of this string is how
 * they stop agreeing.
 */
export const ASSISTANT_PATH = '/admin/ai-assistant';
