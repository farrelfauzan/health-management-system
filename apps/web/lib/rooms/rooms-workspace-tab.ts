/**
 * The tabs of the rooms workspace. A panel that needs to send the user to a
 * sibling tab ("create a ward first") names it with this, not a bare string.
 */
export type RoomsWorkspaceTab = 'occupancy' | 'wards' | 'rooms' | 'beds' | 'classes';
