/**
 * One page of inventory. Matches the billing screens rather than being tuned
 * separately — a clinic reading two admin tables side by side should not have
 * to re-learn how far one page goes.
 */
export const ROOM_INVENTORY_PAGE_SIZE = 10;
